<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="participant">
	<select id="participantInsert" parameterType="participant">
		insert into participant values(seq_participant_no.nextval,#{competitionNo},#{id},default,#{participantContent},null,sysdate)
	</select>
</mapper>
